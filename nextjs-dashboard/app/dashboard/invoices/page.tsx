export default function Page() {
  return <p>Invoices Page</p>;
}

//create new pages on website by making new dir
//in app and adding a page.tsx nice
//only contents returned by page or route are publcily addressable,
//so ui and lib folders don't render anything
